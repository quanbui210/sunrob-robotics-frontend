import LoginForm from "./LoginForm"

export default function LoginPage () {
    return (
        <div style={{textAlign: "center"}}>
            <h1 style={{color: "#333"}}>Login or Signup</h1>
            <LoginForm/>
        </div>
    )
}